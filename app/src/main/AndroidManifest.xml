<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AlunoJiuJitsu"
        tools:targetApi="31">
        <activity
            android:name=".activitys.RecuperarSenha.RecuperarSenha"
            android:exported="false" />
        <activity
            android:name=".activitys.ValidarPagamentoPix.ValidarPagamentoPixEspecie"
            android:exported="false" />
        <activity
            android:name=".activitys.MensalidadePaga.MensalidadePaga"
            android:exported="false"
            android:parentActivityName=".activitys.telaPrincipal.TelaPrincipalAluno"/>
        <activity
            android:name=".activitys.Pagamentos.PagamentoMensalidade"
            android:exported="false"
            />
        <activity
            android:name=".activitys.DetalhesPagamento.DetalhesMensalidade"
            android:exported="false" />
        <activity
            android:name=".activitys.TelaMensalidade.TelaMensalidade"
            android:exported="false"
            android:parentActivityName=".activitys.telaPrincipal.TelaPrincipalAluno"/>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.alunojiujitsu.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".activitys.FormEditarPerfil.FormEditarPerfil"
            android:exported="false" />
        <activity
            android:name=".activitys.telaPrincipal.TelaPrincipalAluno"
            android:exported="false" />
        <activity
            android:name=".activitys.FormCadastro.FormCadastro"
            android:exported="false" />
        <activity
            android:name=".activitys.FormLogin.FormLogin"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".model.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <!--
 Set custom default icon. This is used when no icon is set for incoming notification messages.
     See README(https://goo.gl/l4GJaQ) for more.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/logo" />
        <!--
 Set color used with incoming notification messages. This is used when no color is set for the incoming
             notification message. See README(https://goo.gl/6BKBk7) for more.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/cinza_claro" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />
    </application>

</manifest>